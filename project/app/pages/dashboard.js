export default function Dashboard() {
  return (
    <div>
        <h1>Log in successful, now have access to FBA account information</h1>
        <h2>Dashboard. This is where the data will be displayed (hello from dashboard.JS)</h2>
    </div>
  );
}